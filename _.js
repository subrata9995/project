// let interval = 
let interval =setInterval(() => {
    // let count = context.get("count");
    // count++;
    // context.set("count", count);

    // // send out the updated count
    // node.send({ value: count });
    return"this is a test msg for the greate future";
}, 1000);

